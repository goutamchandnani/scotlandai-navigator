# ScotlandAI Navigator — Backend Dependencies
# Each dependency is here for a specific reason.

# Web framework — async, auto-docs, Pydantic-native
fastapi>=0.115.0

# ASGI server — production-grade
uvicorn[standard]>=0.30.0

# Production process manager — multi-worker for Render
gunicorn>=22.0.0

# Data validation — runtime type checking for discovery answers
pydantic>=2.7.0

# Settings management — loads config from environment variables
pydantic-settings>=2.3.0

# Email validation — required by pydantic's EmailStr type (used in lead capture)
email-validator>=2.1.0

# Async HTTP client — non-blocking Gemini API calls
httpx>=0.27.0

# Google Gemini SDK — AI-powered brief generation
google-genai>=1.0.0

# Signed tokens — 60-minute PDF download links without a database
itsdangerous>=2.2.0

# PDF generation — professional board-ready documents
reportlab>=4.2.0

# Multipart form handling
python-multipart>=0.0.9

# Telegram bot — runs inside FastAPI on Render, always on, no OpenClaw needed
# Uses webhook mode: Telegram pushes updates to /telegram/webhook
python-telegram-bot>=21.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx  # also used as test client for FastAPI
